# aiuis

> Working notes on designing interfaces for systems that think back.

Fallback index served when hosted chapters cannot be listed. The live
`/llms.txt` is generated from published CMS pieces and links each chapter's
markdown (`.md`) version.
