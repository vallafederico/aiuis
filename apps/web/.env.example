SERVER_ENV=""
VITE_CLIENT_ENV=""
# canonical site origin — used for sitemap, robots, og tags
VITE_SITE_URL="https://aiu.is"
CONTENT_PROJECT_ID=project_aiuis
CONTENT_API_URL=https://aiuis.content.software
CONTENT_CDN_URL=https://cdn.content.software/aiuis
# TypeSafe System One (Jev) key for server-side judgments — leave empty to disable
TYPESAFE_API_KEY=""
# Vercel AI Gateway — moodboard mix generation (directed Generative Moodboard)
# and the AI spend / credits panels on Type an Analytic
AI_GATEWAY_API_KEY=""
# Cloudflare API token for Type an Analytic's traffic + Workers AI panels.
# Permissions: Zone → Zone: Read, Zone → Analytics: Read, Account → Account Analytics: Read
CF_API_TOKEN=""
# optional — looked up from VITE_SITE_URL's zone when empty
CF_ZONE_ID=""
CF_ACCOUNT_ID=""
